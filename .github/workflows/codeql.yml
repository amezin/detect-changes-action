name: codeql

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
  schedule:
    - cron: '37 9 * * *'

jobs:
  shared:
    uses: amezin/js-actions-common/.github/workflows/shared-codeql.yml@6e9d841c3ea1f175bfa67d68b2c9f60f5c4f4be3 # v3.0.2
    permissions:
      contents: read
      security-events: write
